[package]
name = "eisen"
version = "0.1.0"
edition = "2024"

[dependencies]
cudarc = { version = "0.19.4", features = ["cuda-version-from-build-system", "driver"] }
rand = "0.8.5"
eisenboard = { path = "eisenboard" }

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
bf16 = []
